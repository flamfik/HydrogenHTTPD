#!/bin/sh
set -e

if command -v systemctl >/dev/null 2>&1; then
    systemctl daemon-reload >/dev/null 2>&1 || true
fi

# Configuration, website content, databases, uploads and logs are intentionally
# preserved. Remove them manually or use the bundled uninstall script with
# --purge when a full cleanup is desired.

exit 0
